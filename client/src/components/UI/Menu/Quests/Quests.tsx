import React, {FC} from 'react';
import cl from './Quests.module.css'

const Quests: FC = () => {
    return (
        <div className={cl.wrap}>
            Quests
        </div>
    );
};

export default Quests;
